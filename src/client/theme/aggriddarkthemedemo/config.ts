import { PredefinedConfig } from 'adaptableblotter/types';

export default {
  Theme: {
    CurrentTheme: 'dark',
  },
  Dashboard: {
    VisibleToolbars: ['AdvancedSearch', 'QuickSearch', 'Theme'],
  },
} as PredefinedConfig;
