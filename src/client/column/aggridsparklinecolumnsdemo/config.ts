import { PredefinedConfig } from '@adaptabletools/adaptable/types';

export default {
  SparklineColumn: {
    SparklineColumns: [
      {
        ColumnId: 'history',
        SparklineType: 'Line',
      },
    ],
  },
} as PredefinedConfig;
