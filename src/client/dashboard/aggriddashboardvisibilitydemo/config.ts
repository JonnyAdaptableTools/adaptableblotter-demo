import { PredefinedConfig } from '@adaptabletools/adaptableblotter/types';

export default {
  Dashboard: {
    DashboardVisibility: 'Minimised',
    HomeToolbarTitle: 'Blotter Toolbar',
  },
} as PredefinedConfig;
