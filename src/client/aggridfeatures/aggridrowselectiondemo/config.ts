import { PredefinedConfig } from '@adaptabletools/adaptableblotter/types';

export default {
  Entitlements: {
    FunctionEntitlements: [
      {
        FunctionName: 'Layout',
        AccessLevel: 'Hidden',
      },
    ],
  },
} as PredefinedConfig;
