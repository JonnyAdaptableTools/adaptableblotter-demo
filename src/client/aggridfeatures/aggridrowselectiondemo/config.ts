import { PredefinedConfig } from '@adaptabletools/adaptable/types';

export default {
  Entitlements: {
    FunctionEntitlements: [
      {
        FunctionName: 'Layout',
        AccessLevel: 'Hidden',
      },
    ],
  },
} as PredefinedConfig;
