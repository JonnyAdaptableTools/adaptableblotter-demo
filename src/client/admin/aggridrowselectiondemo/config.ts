import { PredefinedConfig } from 'adaptableblotter/types';

export default {
  Entitlements: {
    FunctionEntitlements: [
      {
        FunctionName: 'Layout',
        AccessLevel: 'Hidden',
      },
    ],
  },
} as PredefinedConfig;
