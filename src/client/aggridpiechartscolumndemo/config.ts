import { PredefinedConfig } from 'adaptableblotter/types';

import { PredefinedConfig } from 'adaptableblotter/types';

export default {
  Dashboard: {
    VisibleButtons: ['PieChart'],
  },
} as PredefinedConfig;
