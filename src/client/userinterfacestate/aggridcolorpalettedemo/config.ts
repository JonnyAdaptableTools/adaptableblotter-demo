import { PredefinedConfig } from '@adaptabletools/adaptable/types';

export default {
  Dashboard: {
    Tabs: [
      {
        Name: 'Toolbars',
        Toolbars: ['QuickSearch', 'AdvancedSearch'],
      },
    ],
  },
} as PredefinedConfig;
