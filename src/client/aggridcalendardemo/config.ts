import { PredefinedConfig } from 'adaptableblotter/types';

export default {
  Dashboard: {
    VisibleButtons: ['Calendar'],
  },
  Calendar: {
    CurrentCalendar: 'France',
  },
} as PredefinedConfig;
